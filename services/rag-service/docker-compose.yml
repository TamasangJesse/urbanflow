version: "3.9"

services:
  rag-service:
    build: .
    container_name: urbanflow_rag_service
    ports:
      - "8005:8005"
    env_file:
      - .env
    networks:
      - urbanflow_network
    restart: unless-stopped

networks:
  urbanflow_network:
    external: true